//TimesTable(7, 20);

#region Calculating VAT with switch statments and expressions
/*
ConfigureConsole(culture: "fr-FR");

decimal taxToPay = CalculateTax(149, "FR");
WriteLine($"You must pay {taxToPay:C} in tax.");

// alternateively, call the fxn in interpolated string
// WriteLine($"You must pay {CalculateTax(149, "FR"):C} in tax.");
*/
#endregion

#region Converting cardinal numbers to ordinal
/*
RunCardinalToOrdinal();

static void RunCardinalToOrdinal()
{
    for (uint number = 1; number <= 150; number++)
    {
        Write($"{CardinalToOrdinal(number)} "); 
    }
    WriteLine();
}
*/
#endregion

#region Calculating factorials with recursion

RunFactorial();

#endregion

// stopped p. 187